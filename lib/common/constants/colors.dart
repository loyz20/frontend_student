import 'package:flutter/material.dart';

class AppColors {
  // Warna Primer
  static const Color primary = Color(0xFF0A74DA);  // Biru tua sebagai warna primer
  static const Color primaryDark = Color(0xFF003B73);  // Biru lebih gelap untuk aksen

  // Warna Sekunder
  static const Color secondary = Color(0xFFFFA726);  // Oranye sebagai warna sekunder
  static const Color secondaryDark = Color(0xFFFB8C00);  // Oranye lebih gelap untuk aksen

  // Warna Background
  static const Color background = Color(0xFFF5F5F5);  // Abu-abu muda sebagai warna background

  // Warna Text
  static const Color textPrimary = Color(0xFF212121);  // Hitam sebagai warna utama teks
  static const Color textSecondary = Color(0xFF757575);  // Abu-abu untuk teks sekunder

  // Warna Error
  static const Color error = Color(0xFFD32F2F);  // Merah untuk menandakan kesalahan

  // Warna Success
  static const Color success = Color(0xFF388E3C);  // Hijau untuk menandakan keberhasilan

  // Warna Custom
  static const Color customBlue = Color(0xFF021526);  // Warna biru khusus yang diinginkan user

  // Warna Netral
  static const Color white = Colors.white;
  static const Color black = Colors.black;

  // Warna Aksen
  static const Color accent = Color(0xFF448AFF);  // Biru terang sebagai aksen tambahan

  // Warna untuk Borders
  static const Color border = Color(0xFFBDBDBD);  // Abu-abu untuk border/batas
}
