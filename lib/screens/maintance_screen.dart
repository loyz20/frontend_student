import 'package:flutter/material.dart';

class MaintanceScreen extends StatelessWidget {
  const MaintanceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Maintance')),
      body: const Center(child: Text('Maintance Screen')),
    );
  }
}

// Lakukan hal yang sama untuk layar lain
